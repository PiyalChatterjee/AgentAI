# Day 6: Function Calling and Tool Use

Focus: design tool schemas, execute validated tools, and complete model-to-tool-to-model loop safely.

Mode: mentor-led interactive build. The main script will be written step by step during the session.

## Files
- DAY_06_NOTES.md: Q1-Q4 answers and A1-A8 evidence
- day6_function_calling.py: to be built interactively (do not pre-generate)

## Quick Start
```powershell
cd day_06
code DAY_06_NOTES.md
```

## Azure Setup
The script expects these env variables in root `.env`:
- AZURE_OPENAI_API_KEY
- AZURE_OPENAI_ENDPOINT
- AZURE_OPENAI_API_VERSION
- AZURE_OPENAI_DEPLOYMENT_NAME

If Azure is unavailable, we will add a local fallback path during the guided build.

## Interactive Commands
- We will define commands and execution flow during implementation.
